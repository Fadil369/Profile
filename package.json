{
  "name": "elfadil-profile",
  "private": true,
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "3.1.1"
  },
  "scripts": {
    "prepare": "husky",
    "build": "npm --prefix landing ci && npm --prefix landing run build",
    "deploy": "npm run build && npx wrangler deploy"
  },
  "lint-staged": {
    "*.{js,css,md,json}": "prettier --write"
  }
}
